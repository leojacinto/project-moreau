/**
 * MCP Tool Handlers — expose ontology queries and constraint
 * evaluation as callable tools with semantic metadata.
 *
 * Tools are registered directly in index.ts using Zod schemas
 * with the McpServer high-level API. This module is reserved
 * for shared tool utilities if needed in the future.
 */
